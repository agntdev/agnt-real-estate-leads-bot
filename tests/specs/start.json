[
  {
    "name": "/start replies with the welcome + main menu",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Share your property plans and an agent will contact you. Tap 'Submit request' to begin." }
          }
        ]
      }
    ]
  },
  {
    "name": "a non-owner cannot open leads from the owner start shortcut",
    "steps": [
      {
        "send": { "callback": "view_leads" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Owner access isn't set up yet." }
          }
        ]
      }
    ]
  }
]
